% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getRandomMask.R
\name{getRandomMask}
\alias{getRandomMask}
\title{Generate a Random Mask for a List of Sparse Matrices}
\usage{
getRandomMask(dataList, ratio, seed = NULL)
}
\arguments{
\item{dataList}{A list of sparse matrices (each of class \code{dgCMatrix}) to be aligned.}

\item{ratio}{A numeric value (between 0 and 1) specifying the proportion of \code{TRUE} values (non-masked elements).}

\item{seed}{An optional numeric value to set the random seed for reproducibility (default: NULL).}
}
\value{
A sparse logical matrix (\code{lgCMatrix}) with \code{TRUE} (non-masked) and \code{FALSE} (masked) values.
}
\description{
Creates a logical sparse matrix (\code{lgCMatrix}) with \code{TRUE/FALSE} values,
aligning multiple matrices based on their unique cell names and randomly masking entries.
}
\details{
This function aligns a list of sparse matrices by mapping them to a shared set of unique cells (column names).
It then applies a random mask based on the specified \code{ratio}, ensuring row names are properly assigned.
}
\examples{
library(Matrix)

# Create example sparse matrices
mat1 <- Matrix(sample(c(TRUE, FALSE), 20, replace = TRUE), nrow = 4, sparse = TRUE)
colnames(mat1) <- c("A", "B", "C", "D", "E")
rownames(mat1) <- paste0("R", 1:4)

mat2 <- Matrix(sample(c(TRUE, FALSE), 15, replace = TRUE), nrow = 3, sparse = TRUE)
colnames(mat2) <- c("A", "B", "C", "D", "F")
rownames(mat2) <- paste0("R", 5:7)

dataList <- list(mat1, mat2)

# Generate a random mask with 20\% sparsity
mask <- getRandomMask(dataList, ratio = 0.2, seed = 42)

print(mask)

}
