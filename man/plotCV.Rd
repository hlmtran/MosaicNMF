% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotCV.R
\name{plotCV}
\alias{plotCV}
\title{Plot Cross-Validation Results for NMF}
\usage{
plotCV(data)
}
\arguments{
\item{data}{A \code{data.frame} with columns \code{rep}, \code{k}, and \code{value}, where:
\itemize{
\item \code{rep} indicates the replicate (factor or integer),
\item \code{k} is the rank of factorization,
\item \code{value} is the prediction error (typically mean squared error).
}}
}
\value{
A \code{ggplot} object showing line plots of prediction error vs. rank (\code{k}) for each replicate.
}
\description{
Visualizes the prediction error across different NMF ranks (\code{k}), grouped by replicate (\code{rep}), from cross-validation results.
}
\details{
This function is useful for visualizing the results of non-negative matrix factorization (NMF) cross-validation, where the optimal rank minimizes prediction error.
}
\examples{
\dontrun{
df <- data.frame(
  rep = as.factor(rep(1:3, each = 3)),
  k = rep(3:5, times = 3),
  value = runif(9, 0, 0.05)
)
plotCV(df)
}

}
